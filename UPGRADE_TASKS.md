# Upgrade Tasks — Live Tracker

Status legend: 🔲 todo · 🟡 in-progress · ✅ done · ⛔ blocked

Update this file after every task. One task = one commit minimum.

---

## P1 — Per-component RUL

| ID   | Status | Description                                                                                          | Files touched                                                                                  | Depends on |
|------|--------|------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|------------|
| P1.1a | ✅    | Bump default training trajectory length 3000 → 15000 cycles. Nothing else. — `--cycles` default flipped to 15000 in [scripts/generate_training_data.py](scripts/generate_training_data.py); no other behavior changed.                            | [scripts/generate_training_data.py](scripts/generate_training_data.py)                          | —          |
| P1.1b | ✅    | Bias fault sampler toward `drive`/`mold` via 1.5× normalized weights (`_BIAS_COMPONENTS={"drive","mold"}`). Plus: `simulate_machine(..., machine_index=i)` deterministically cycles through all 5 faults across training machines — this is what actually unblocked P1.4. | [scripts/generate_training_data.py](scripts/generate_training_data.py)                          | P1.4       |
| P1.2 | ✅     | Emit one `rul_<comp>_cycles` column per FSM component; keep `rul_cycles = min(...)` for back-compat. — Added per-component columns (`rul_hydraulic_cycles`, `rul_screw_check_ring_cycles`, `rul_heaters_cycles`, `rul_drive_cycles`, `rul_mold_cycles`) alongside existing `rul_cycles` min. | [scripts/generate_training_data.py](scripts/generate_training_data.py)                          | P1.1a      |
| P1.3 | ✅     | Refactor `train()` to loop over components; new artifact `{components: {<comp>: {models, feature_cols, val_scores}}}`. Keep GroupKFold-by-machine. — Looped over 5 FSM components (sourced from `BASE_WEAR`); per-component finite-mask on RUL targets; GroupKFold-by-machine preserved (`n_splits=min(5,n_machines)`); artifact reshaped under `components/<comp>`. Note: `scripts/retrain_models.py` model_card writer still references the old top-level `r_art['val_scores']` — will break at P1.8 unless patched in P1.5. | [src/ml/train_rul.py](src/ml/train_rul.py)                                                     | P1.2       |
| P1.4 | ✅     | Calibration test: held-out `[p10,p90]` coverage ≥ 70% per component. Final values: hydraulic 0.880, screw_check_ring 0.876, heaters 0.726, drive 0.739, mold 0.735 (FastGBR n_est=40, 10 train machines × 800 cycles, deterministic per-machine fault via `machine_index`). | [tests/test_rul_calibration.py](tests/test_rul_calibration.py)                                  | P1.3       |
| P1.5 | ✅     | Predictor loads per-component artifact, emits `rul_per_component` (p10/p50/p90 per FSM comp) + back-compat top-level `rul` from worst-by-p50 component. Sort-enforced quantile monotonicity in new `predict_rul_per_component(feats, artifact)` helper. Patched `scripts/retrain_models.py` model_card writer for new artifact shape. | [src/ml/predict.py](src/ml/predict.py), [scripts/retrain_models.py](scripts/retrain_models.py) | P1.3       |
| P1.6 | ✅     | `rul_per_component` block added to snapshot (with per-cycle FSM fallback when ML disabled); top-level `rul` exposes `worst_component`, `replacement_date`, `urgency` (last two added after a runtime smoke-run found them dropped during the field-by-field rebuild). Verified live via `/api/snapshot`. | [run.py](run.py)                                                                                 | P1.5       |
| P1.7 | ✅     | Schema/sanity test (4 tests): 5 components × 3 quantiles, p10≤p50≤p90 (via `predict_rul_per_component` to absorb GBR crossings), forbidden FSM-state tokens (`health`, `fault_pressure`, `rul_`, `fsm`) absent from feature_cols. | [tests/test_rul_per_component.py](tests/test_rul_per_component.py)                              | P1.5       |
| P1.8 | ✅     | Regenerated 80K-row dataset (20 machines, all per-component RUL cols) + retrained both models end-to-end at production config. **Perf:** added opt-in `n_jobs` to `train_rul.train()` (parallel over 5 components) and `train_quality.train()` (parallel calibration CV); `retrain_models.py` now passes n_jobs=5/3 (default stays 1 so P1.4/P1.7 monkeypatch is unaffected). **Bugfix:** `train_quality.py` was leaking the 5 `rul_<comp>_cycles` columns (FSM-derived) as features (54→49 cols) — excluded all `rul_*` now; retrained. Smoke-tested predict.py: 5 distinct ordered bands + replacement_date/urgency + quality `good`, no KeyError. | [scripts/retrain_models.py](scripts/retrain_models.py), [src/ml/train_rul.py](src/ml/train_rul.py), [src/ml/train_quality.py](src/ml/train_quality.py), `artifacts/models/*` | P1.3, P1.4 |
| P1.9 | ✅     | A-T3 AST lint (`features.py` does not import `src.simulator.*`) green in `tests/test_simulator.py` run. NOTE: lint does NOT catch training-time RUL-column leakage (found via smoke test, fixed in P1.8). Test_at5_heater_drift failure is pre-existing/stochastic (baseline 5.56 ≈ drifted 5.53 std), unrelated to P1. | (lint script)                                                                                    | P1.3       |

## P2 — Replacement dates + urgency

| ID   | Status | Description                                                                                          | Files touched                                                                                   | Depends on |
|------|--------|------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------|------------|
| P2.1 | ✅     | Pure functions `cycles_to_date(cycles, cycles_per_day, now)`, `classify(days)` (bands <7 critical / 7–30 imminent / 30–90 schedule / ≥90 monitor), and `days_until(target, now)` helper.        | [src/ml/urgency.py](src/ml/urgency.py)                                                          | —          |
| P2.2 | ✅     | 17 unit tests: day math (whole, fractional, zero, negative cycles), 8 band-edge parametrizations, error cases for cycles_per_day≤0, days_until past+future. All pass. | [tests/test_urgency.py](tests/test_urgency.py)                                                  | P2.1       |
| P2.3 | ✅     | `predict()` accepts `cycles_per_day` + `now` kwargs; each `rul_per_component` entry gains `replacement_date` (ISO date), `days_until_replacement`, `urgency`. Top-level `rul` echoes the worst-component date+urgency. | [src/ml/predict.py](src/ml/predict.py)                                                           | P1.5, P2.1 |
| P2.4 | ✅     | `CONFIG = {"cycles_per_day": 4000}` module-global; `GET /api/settings` returns it, `POST /api/settings` validates+updates `cycles_per_day` (>0, numeric). Snapshot now includes a `config` block; `_build_snapshot` passes `cycles_per_day` through to `ml_predict`. | [run.py](run.py)                                                                                 | P2.3       |

## P3 — UI modernization (Vite + React + Tailwind + shadcn + Recharts + lucide)

| ID    | Status | Description                                                                                          | Files touched                                                                                  | Depends on |
|-------|--------|------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|------------|
| P3.1  | ✅     | Added `@vitejs/plugin-react`, React 18, Tailwind 3.4, shadcn runtime deps (cva/clsx/twMerge/animate, radix dialog+tabs+tooltip+slot), Recharts 2.12, lucide-react, `@types/node`. Vite config now uses React plugin + `@` alias.                       | [web/package.json](web/package.json), [web/vite.config.ts](web/vite.config.ts)                  | —          |
| P3.2  | ✅     | Tailwind v3.4 + PostCSS configured; `src/index.css` defines slate-900 dark theme with **amber-500** primary/accent/ring via shadcn HSL variables. Old `styles.css` deleted.                                | [web/tailwind.config.js](web/tailwind.config.js), [web/postcss.config.js](web/postcss.config.js), [web/src/index.css](web/src/index.css)                            | P3.1       |
| P3.3  | ✅     | Hand-wrote shadcn New York primitives (card, badge, button, dialog, tabs, tooltip) under `web/src/components/ui/` + `lib/utils.ts` (`cn` helper). Avoided `npx shadcn init` since it requires interactive prompts. | `web/src/components/ui/*`, [web/src/lib/utils.ts](web/src/lib/utils.ts)                          | P3.2       |
| P3.4  | ✅     | `main.tsx` mounts `<App />`; `useSnapshot()` hook wraps `IMMClient` with a module-singleton WS; `api.ts` types updated to new snapshot shape (added `rul_per_component`, `RulPerComponentEntry`, `Urgency`, `config`, `worst_component`, `replacement_date`, `urgency`) — runtime untouched (also added `offSnapshot` for cleanup). | [web/src/main.tsx](web/src/main.tsx), [web/src/hooks/useSnapshot.ts](web/src/hooks/useSnapshot.ts), [web/src/App.tsx](web/src/App.tsx), [web/src/api.ts](web/src/api.ts) | P3.3       |
| P3.5  | ✅     | `QualityCard.tsx` — shadcn `Badge` pill + Tailwind probability bars.                                                                    | [web/src/components/QualityCard.tsx](web/src/components/QualityCard.tsx)                         | P3.4       |
| P3.6  | ✅     | `FaultButtons.tsx` — controlled toggle buttons driven by `activeFaults` prop; calls `onInject(id)` / `onInject("clear_"+id)`.                                                                   | [web/src/components/FaultButtons.tsx](web/src/components/FaultButtons.tsx)                       | P3.4       |
| P3.7  | ✅     | `ProcessCharts.tsx` — 5 Recharts `LineChart`s with rolling 100-point window kept in a ref, force-rerender via tick counter.                                           | [web/src/components/ProcessCharts.tsx](web/src/components/ProcessCharts.tsx)                     | P3.4       |
| P3.8  | ✅     | `RULBand.tsx` — Recharts `AreaChart` with p10/p90 band via array-valued Area, p50 line, `ReferenceArea` for optimal window, `ReferenceLine` for failure threshold. Accepts `history` prop for component-scoped reuse.                                   | [web/src/components/RULBand.tsx](web/src/components/RULBand.tsx)                                 | P3.4       |
| P3.9  | ✅     | `Twin2D.tsx` — SVG re-rendered as React; pulse driven by `activeFaults` effect.                                               | [web/src/components/Twin2D.tsx](web/src/components/Twin2D.tsx)                                   | P3.4       |
| P3.10 | ✅     | Deleted `main.ts`, `Twin2D.ts`, `HealthBars.ts`, `FaultButtons.ts`, `RULBand.ts`, `QualityCard.ts`, `ProcessCharts.ts`, `styles.css`. `index.html` now mounts `#root` and loads `/src/main.tsx`.                       | [web/](web/)                                                                                     | P3.5–P3.9, P4.1 |

## P4 — Radial gauges (replaces HealthBars)

| ID   | Status | Description                                                                                          | Files touched                                                                                   | Depends on |
|------|--------|------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------|------------|
| P4.1 | ✅     | `ComponentGauge.tsx` — `RadialBarChart` with 270° sweep (startAngle=225 → endAngle=-45), bar value = health %, ring color from `URGENCY_RING` map (critical=red-500, imminent=amber-500, schedule=cyan-500, monitor=emerald-500). Center text shows `{days}d` + label below. | [web/src/components/ComponentGauge.tsx](web/src/components/ComponentGauge.tsx) (new)            | P3.4, P2.4 |
| P4.2 | ✅     | Whole gauge is a button → shadcn `<Dialog>` shows urgency badge, p10/p50/p90 stat trio, a component-scoped `<RULBand>` (single-point history from the entry, plus the global failure/optimal refs), replacement date, days-remaining, and a stub `<Button>` with `Wrench` icon labelled "Schedule maintenance" that only closes the dialog.              | [web/src/components/ComponentGauge.tsx](web/src/components/ComponentGauge.tsx)                  | P4.1, P3.8 |
| P4.3 | ✅     | `ComponentGrid.tsx` lays out 5 `ComponentGauge`s in a responsive grid (2/3/5 cols), iterating `["hydraulic","screw_check_ring","heaters","drive","mold"]`; `LABELS["screw_check_ring"] = "Screw / Check Ring"`.                       | [web/src/components/ComponentGrid.tsx](web/src/components/ComponentGrid.tsx) (new)               | P4.1       |
| P4.4 | ✅     | `App.tsx` mounts `<ComponentGrid />` in a "Component Health & RUL" card (right column, top). `HealthBars.ts` deleted with the other legacy files in P3.10.                       | [web/src/App.tsx](web/src/App.tsx) | P4.3       |

## P5 — 3D backdrop (⛔ gated)

| ID   | Status | Description                                                                                          | Files touched                                                                                  | Depends on |
|------|--------|------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|------------|
| P5.0 | ✅     | Asset present at `web/public/models/injection_machine.zip`. Inspected: 179 nodes, all generic names (`group_*`, `instance_*`) — no semantic per-component nodes → live data binding out of scope, static backdrop only. | —                                                                                              | user upload |
| P5.1 | ✅     | License check done — **outcome: CLEARED**. Owner confirmed source = Trimble 3D Warehouse model `9db581d85786c7251fd4c0ca94ea0bfa`. General Model License permits embedding in a "Combined Work" with substantial additional content (our app qualifies), permits format conversion/decimation, requires no attribution; only standalone redistribution/mapping-aggregation is barred (N/A). Full analysis in doc. | [docs/3D_LICENSE.md](docs/3D_LICENSE.md)                                                  | P5.0       |
| P5.2 | ✅     | Converted via trimesh (`.dae`→raw glb, 1223 meshes/110K verts/95K faces, 3.97 MB) then `@gltf-transform/cli optimize --compress draco --texture-compress webp` (dedup/join/weld/simplify/Draco/WebP) → **350 KB (307 KB gzipped)**, well under the 5 MB target. Extensions: KHR_draco_mesh_compression, EXT_texture_webp, EXT_mesh_gpu_instancing. | `web/public/models/injection_machine.glb`                                                        | P5.1       |
| P5.3 | ✅     | Final form (after visual review with owner): **dedicated interactive panel** `Twin3D.tsx` instead of a background layer — plain Three.js, GLTFLoader + DRACOLoader (decoder served from `/draco/`), **OrbitControls** (drag-orbit, scroll-zoom, idle auto-rotate), bbox auto-fit, ResizeObserver, full dispose-on-unmount. Header comment present. Initial `ThreeBackdrop.tsx` (opacity backdrop) was built, verified, then removed in favor of this. Verified in-app: canvas mounts, Draco decodes, 0 console errors. | [web/src/components/Twin3D.tsx](web/src/components/Twin3D.tsx)              | P5.2, P3.4 |
| P5.4 | ✅     | Feature-flag `const ENABLE_3D = import.meta.env.VITE_ENABLE_3D === "true"`; when ON, the Digital Twin card renders `<Twin3D/>` (else the existing `<Twin2D/>` SVG). Default OFF (lightweight build, no three.js model fetch). Added `web/src/vite-env.d.ts` for env typing. **Also patched run.py** to serve `/models` + `/draco` static dirs (built backend served only `/assets`, so the glb/decoder 404'd from `dist`). Build/run with `VITE_ENABLE_3D=true` to enable. | [web/src/App.tsx](web/src/App.tsx), [web/src/vite-env.d.ts](web/src/vite-env.d.ts), [run.py](run.py)                            | P5.3       |

---

## Cross-cutting notes

- **Spine:** P1 → P2 sequential. P3 may run in parallel with P2 once P1 is merged. P4 depends on P3 + P2. P5 independent, gated on P5.0.
- **AST lint** (no FSM state as ML features) must remain green after every P1 task — re-run as part of P1.9.
- **GroupKFold-by-machine** is preserved in P1.3; do not switch validation strategy.
- **DataSource abstraction** in [src/datasource/](src/datasource/) is untouched by this upgrade.
- **No new ML heads** (anomaly, SHAP, cost-aware policy) — explicitly out of scope.
- **Cut order if scope blows up:** P5 → P4 → P3 → P2 → P1.
